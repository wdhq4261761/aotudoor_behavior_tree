# 独立行为树系统设计方案

## 版本信息

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 创建日期 | 2026-04-09 |
| 适用范围 | 独立行为树系统 |

---

## 1. 项目概述

### 1.1 项目目标

基于AutoDoor项目中的行为树系统，开发一个独立的、自包含的行为树解决方案，具备以下特性：

- **混合模式**：核心库 + 可选GUI组件，既可以作为库使用，也可以作为独立应用运行
- **完整功能**：包含所有节点类型（组合/条件/动作节点）以及脚本录制功能
- **独立运行**：不依赖原始项目基础设施，可独立构建和运行

### 1.2 核心特性

- **可视化编辑器**：提供直观的拖拽式节点编辑界面
- **丰富的节点类型**：支持组合节点、条件节点、动作节点三大类
- **装饰器机制**：内置重试、重复、超时、取反等装饰参数
- **黑板系统**：支持节点间数据共享与通信
- **多格式序列化**：支持 JSON、YAML、TEXT 三种格式
- **撤销/重做**：完整的编辑历史管理
- **脚本录制**：支持操作录制和回放

---

## 2. 项目结构

```
behavior_tree_standalone/
├── main.py                      # 应用入口
├── requirements.txt             # 依赖清单
├── README.md                    # 项目说明
├── setup.py                     # 安装配置
│
├── bt_core/                     # 核心库（可独立使用）
│   ├── __init__.py
│   ├── nodes.py                 # 节点基类与组合节点
│   ├── engine.py                # 执行引擎
│   ├── context.py               # 执行上下文
│   ├── blackboard.py            # 黑板系统
│   ├── serializer.py            # 序列化器
│   ├── status.py                # 节点状态定义
│   └── registry.py              # 节点注册中心
│
├── bt_nodes/                    # 节点扩展库（可选）
│   ├── __init__.py
│   ├── actions/                 # 动作节点
│   │   ├── __init__.py
│   │   ├── keyboard.py          # 按键节点
│   │   ├── mouse.py             # 鼠标节点
│   │   ├── delay.py             # 延时节点
│   │   ├── variable.py          # 变量节点
│   │   ├── script.py            # 脚本节点
│   │   ├── code.py              # 代码节点
│   │   └── alarm.py             # 报警节点
│   │
│   └── conditions/              # 条件节点
│       ├── __init__.py
│       ├── ocr.py               # OCR检测节点
│       ├── image.py             # 图像检测节点
│       ├── color.py             # 颜色检测节点
│       ├── number.py            # 数字检测节点
│       └── variable.py          # 变量判断节点
│
├── bt_utils/                    # 工具模块
│   ├── __init__.py
│   ├── screenshot.py            # 截图管理器
│   ├── input_controller.py      # 输入控制器
│   ├── ocr_manager.py           # OCR管理器
│   ├── image_processor.py       # 图像处理器
│   └── recorder.py              # 脚本录制器
│
├── bt_gui/                      # GUI组件（可选）
│   ├── __init__.py
│   ├── app.py                   # 主应用窗口
│   ├── editor/                  # 编辑器组件
│   │   ├── __init__.py
│   │   ├── editor.py            # 编辑器主组件
│   │   ├── canvas.py            # 画布组件
│   │   ├── palette.py           # 节点面板
│   │   ├── property.py          # 属性面板
│   │   ├── toolbar.py           # 工具栏
│   │   ├── connection.py        # 连接线管理
│   │   └── undo_redo.py         # 撤销/重做
│   │
│   ├── dialogs/                 # 对话框
│   │   ├── __init__.py
│   │   ├── region_selector.py   # 区域选择器
│   │   └── settings.py          # 设置对话框
│   │
│   └── theme.py                 # 主题配置
│
├── config/                      # 配置文件
│   └── settings.json            # 默认设置
│
├── assets/                      # 资源文件
│   ├── icons/                   # 图标
│   └── sounds/                  # 音效
│
└── examples/                    # 示例文件
    ├── demo_tree.json           # 示例行为树
    └── demo_script.txt          # 示例脚本
```

---

## 3. 系统架构

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           独立行为树系统架构                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    GUI层 (bt_gui) - 可选                         │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐               │   │
│  │  │ Editor  │ │ Canvas  │ │ Palette │ │Property │               │   │
│  │  └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘               │   │
│  └───────┼───────────┼───────────┼───────────┼─────────────────────┘   │
│          │           │           │           │                          │
│          └───────────┴─────┬─────┴───────────┘                          │
│                              │                                          │
│  ┌───────────────────────────┼──────────────────────────────────────┐   │
│  │                    节点扩展层 (bt_nodes) - 可选                    │   │
│  │  ┌───────────────────────┴───────────────────────┐               │   │
│  │  │                                                 │               │   │
│  │  │  ┌─────────────┐         ┌─────────────┐      │               │   │
│  │  │  │  Actions    │         │ Conditions  │      │               │   │
│  │  │  │ 按键/点击/   │         │ OCR/图像/   │      │               │   │
│  │  │  │ 延时/脚本   │         │ 颜色/数字   │      │               │   │
│  │  │  └──────┬──────┘         └──────┬──────┘      │               │   │
│  │  │         │                       │              │               │   │
│  │  └─────────┼───────────────────────┼──────────────┘               │   │
│  │            │                       │                               │   │
│  └────────────┼───────────────────────┼───────────────────────────────┘   │
│               │                       │                                   │
│  ┌────────────┼───────────────────────┼───────────────────────────────┐   │
│  │            │    核心层 (bt_core)    │                               │   │
│  │  ┌─────────┴───────────────────────┴─────────┐                     │   │
│  │  │              Node (抽象基类)               │                     │   │
│  │  └───────────────────┬───────────────────────┘                     │   │
│  │                      │                                             │   │
│  │       ┌──────────────┼──────────────┐                             │   │
│  │       │              │              │                             │   │
│  │  ┌────┴────┐   ┌─────┴─────┐  ┌────┴────┐                        │   │
│  │  │Composite│   │Condition  │  │ Action  │                        │   │
│  │  │ 组合节点 │   │ 条件节点  │  │动作节点 │                        │   │
│  │  └────┬────┘   └─────┬─────┘  └────┬────┘                        │   │
│  │       │              │              │                             │   │
│  │  ┌────┴────┐   ┌─────┴─────┐  ┌────┴────┐                        │   │
│  │  │Sequence │   │  基类     │  │  基类   │                        │   │
│  │  │Selector │   └───────────┘  └─────────┘                        │   │
│  │  │Parallel │                                                    │   │
│  │  └─────────┘                                                    │   │
│  │                                                                  │   │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐                │   │
│  │  │   Engine    │ │ Blackboard  │ │ Serializer  │                │   │
│  │  │  执行引擎   │ │   黑板      │ │  序列化器   │                │   │
│  │  └─────────────┘ └─────────────┘ └─────────────┘                │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    工具层 (bt_utils)                              │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐            │   │
│  │  │Screenshot│ │  Input   │ │   OCR    │ │ Recorder │            │   │
│  │  │  截图    │ │  输入    │ │  识别    │ │  录制    │            │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘            │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.2 层次说明

| 层次 | 模块 | 说明 | 可选性 |
|------|------|------|--------|
| GUI层 | bt_gui | 可视化编辑器界面 | 可选 |
| 节点扩展层 | bt_nodes | 动作节点和条件节点实现 | 可选 |
| 核心层 | bt_core | 行为树核心框架 | 必需 |
| 工具层 | bt_utils | 截图、输入、OCR等工具 | 可选 |

---

## 4. 核心组件设计

### 4.1 节点系统 (bt_core/nodes.py)

#### 4.1.1 节点状态定义

```python
from enum import Enum

class NodeStatus(Enum):
    """节点执行状态"""
    SUCCESS = "success"    # 执行成功
    FAILURE = "failure"    # 执行失败
    RUNNING = "running"    # 正在执行
    ABORTED = "aborted"    # 被中止
```

#### 4.1.2 节点配置类

```python
from dataclasses import dataclass

@dataclass
class NodeConfig:
    """节点配置"""
    name: str = ""
    description: str = ""
    enabled: bool = True
    retry_count: int = 0      # 重试次数（-1无限）
    repeat_count: int = 0     # 重复次数（-1无限）
    timeout_ms: int = 0       # 超时时间（毫秒）
```

#### 4.1.3 节点基类

```python
from abc import ABC, abstractmethod
from typing import List, Dict, Any, Optional

class Node(ABC):
    """节点抽象基类"""
    
    def __init__(self, node_id: str, config: NodeConfig = None):
        self.node_id = node_id
        self.config = config or NodeConfig()
        self.status = NodeStatus.SUCCESS
        self.children: List["Node"] = []
        self.parent: Optional["Node"] = None
    
    @abstractmethod
    def tick(self, context: "ExecutionContext") -> NodeStatus:
        """执行节点逻辑"""
        pass
    
    def reset(self) -> None:
        """重置节点状态"""
        self.status = NodeStatus.SUCCESS
        for child in self.children:
            child.reset()
    
    def abort(self, context: "ExecutionContext") -> None:
        """中止节点执行"""
        self.reset()
    
    def add_child(self, child: "Node") -> None:
        """添加子节点"""
        child.parent = self
        self.children.append(child)
    
    def remove_child(self, child: "Node") -> None:
        """移除子节点"""
        if child in self.children:
            self.children.remove(child)
            child.parent = None
    
    def to_dict(self) -> Dict[str, Any]:
        """序列化为字典"""
        return {
            "id": self.node_id,
            "type": self.__class__.__name__,
            "name": self.config.name,
            "description": self.config.description,
            "enabled": self.config.enabled,
            "config": {
                "retry_count": self.config.retry_count,
                "repeat_count": self.config.repeat_count,
                "timeout_ms": self.config.timeout_ms,
            },
            "children": [child.node_id for child in self.children],
        }
    
    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> "Node":
        """从字典反序列化"""
        pass
```

#### 4.1.4 组合节点基类

```python
class CompositeNode(Node):
    """组合节点基类"""
    
    def __init__(self, node_id: str, config: NodeConfig = None):
        super().__init__(node_id, config)
        self.current_index = 0  # 当前执行位置
        self._start_time = None
```

#### 4.1.5 顺序节点

```python
class SequenceNode(CompositeNode):
    """
    顺序节点：按顺序依次执行所有子节点
    - 所有子节点成功才返回成功
    - 任一子节点失败立即返回失败（默认）
    - 支持 continue_on_failure 参数继续执行
    """
    
    def tick(self, context: "ExecutionContext") -> NodeStatus:
        if not self.children:
            return NodeStatus.SUCCESS
        
        # 检查超时
        if self.config.timeout_ms > 0:
            if context.elapsed_time * 1000 >= self.config.timeout_ms:
                return NodeStatus.FAILURE
        
        has_failure = False
        
        while self.current_index < len(self.children):
            child = self.children[self.current_index]
            
            if not child.config.enabled:
                self.current_index += 1
                continue
            
            status = child.tick(context)
            
            if status == NodeStatus.RUNNING:
                return NodeStatus.RUNNING
            
            if status == NodeStatus.FAILURE:
                if self.config.get("continue_on_failure", False):
                    has_failure = True
                    self.current_index += 1
                    continue
                return NodeStatus.FAILURE
            
            self.current_index += 1
        
        self.current_index = 0
        return NodeStatus.FAILURE if has_failure else NodeStatus.SUCCESS
```

#### 4.1.6 选择节点

```python
class SelectorNode(CompositeNode):
    """
    选择节点：按顺序执行子节点，任一成功即返回成功
    """
    
    def tick(self, context: "ExecutionContext") -> NodeStatus:
        if not self.children:
            return NodeStatus.FAILURE
        
        while self.current_index < len(self.children):
            child = self.children[self.current_index]
            
            if not child.config.enabled:
                self.current_index += 1
                continue
            
            status = child.tick(context)
            
            if status == NodeStatus.RUNNING:
                return NodeStatus.RUNNING
            
            if status == NodeStatus.SUCCESS:
                self.current_index = 0
                return NodeStatus.SUCCESS
            
            self.current_index += 1
        
        self.current_index = 0
        return NodeStatus.FAILURE
```

#### 4.1.7 并行节点

```python
class ParallelNode(CompositeNode):
    """
    并行节点：同时执行所有子节点
    - require_all: 所有子节点成功才成功
    - require_one: 任一子节点成功即成功
    """
    
    def __init__(self, node_id: str, config: NodeConfig = None):
        super().__init__(node_id, config)
        self.cached_statuses: Dict[int, NodeStatus] = {}
        self.success_policy = config.get("success_policy", "require_all") if config else "require_all"
    
    def tick(self, context: "ExecutionContext") -> NodeStatus:
        if not self.children:
            return NodeStatus.SUCCESS
        
        success_count = 0
        failure_count = 0
        running_children = []
        enabled_count = 0
        
        for i, child in enumerate(self.children):
            if not child.config.enabled:
                continue
            
            enabled_count += 1
            
            # 检查缓存状态
            if i in self.cached_statuses:
                status = self.cached_statuses[i]
                if status == NodeStatus.SUCCESS:
                    success_count += 1
                    continue
                elif status == NodeStatus.FAILURE:
                    failure_count += 1
                    continue
            
            status = child.tick(context)
            
            if status == NodeStatus.SUCCESS:
                self.cached_statuses[i] = NodeStatus.SUCCESS
                success_count += 1
            elif status == NodeStatus.FAILURE:
                self.cached_statuses[i] = NodeStatus.FAILURE
                failure_count += 1
            elif status == NodeStatus.RUNNING:
                running_children.append(child)
        
        # require_one 策略
        if self.success_policy == "require_one" and success_count > 0:
            self._abort_running(context, running_children)
            return NodeStatus.SUCCESS
        
        if running_children:
            return NodeStatus.RUNNING
        
        # require_all 策略
        if self.success_policy == "require_all":
            return NodeStatus.SUCCESS if success_count == enabled_count else NodeStatus.FAILURE
        else:
            return NodeStatus.SUCCESS if success_count > 0 else NodeStatus.FAILURE
    
    def _abort_running(self, context: "ExecutionContext", children: List[Node]) -> None:
        for child in children:
            if child.status == NodeStatus.RUNNING:
                child.abort(context)
    
    def reset(self) -> None:
        super().reset()
        self.cached_statuses.clear()
```

#### 4.1.8 条件节点基类

```python
class ConditionNode(Node):
    """条件节点基类"""
    
    def __init__(self, node_id: str, config: NodeConfig = None):
        super().__init__(node_id, config)
        self.invert = config.get("invert", False) if config else False
        self.check_interval_ms = config.get("check_interval_ms", 300) if config else 300
        self._last_check_time = 0
    
    def tick(self, context: "ExecutionContext") -> NodeStatus:
        # 检查间隔
        current_time = context.elapsed_time * 1000
        if current_time - self._last_check_time < self.check_interval_ms:
            return self.status
        
        self._last_check_time = current_time
        
        # 执行条件检测
        result = self._check_condition(context)
        
        # 取反处理
        if self.invert:
            result = not result
        
        status = NodeStatus.SUCCESS if result else NodeStatus.FAILURE
        self.status = status
        
        # 条件成功时执行子节点
        if status == NodeStatus.SUCCESS and self.children:
            return self._execute_children(context)
        
        return status
    
    @abstractmethod
    def _check_condition(self, context: "ExecutionContext") -> bool:
        """检测条件是否满足"""
        pass
    
    def _execute_children(self, context: "ExecutionContext") -> NodeStatus:
        """执行子节点"""
        for child in self.children:
            status = child.tick(context)
            if status != NodeStatus.SUCCESS:
                return status
        return NodeStatus.SUCCESS
```

#### 4.1.9 动作节点基类

```python
class ActionNode(Node):
    """动作节点基类"""
    
    def tick(self, context: "ExecutionContext") -> NodeStatus:
        # 执行动作
        status = self._execute_action(context)
        self.status = status
        
        # 动作成功时执行子节点
        if status == NodeStatus.SUCCESS and self.children:
            return self._execute_children(context)
        
        return status
    
    @abstractmethod
    def _execute_action(self, context: "ExecutionContext") -> NodeStatus:
        """执行动作"""
        pass
    
    def _execute_children(self, context: "ExecutionContext") -> NodeStatus:
        """执行子节点"""
        for child in self.children:
            status = child.tick(context)
            if status != NodeStatus.SUCCESS:
                return status
        return NodeStatus.SUCCESS
```

---

### 4.2 执行引擎 (bt_core/engine.py)

```python
import threading
import time
from typing import Optional, Callable, Dict, Any

class BehaviorTreeEngine:
    """行为树执行引擎"""
    
    def __init__(self, root_node: Node = None):
        self.root_node = root_node
        self.context: Optional[ExecutionContext] = None
        self._running = False
        self._paused = False
        self._thread: Optional[threading.Thread] = None
        self._tick_interval = 0.05  # 50ms
        self._on_status_change: Optional[Callable] = None
        self._on_node_status: Optional[Callable] = None
    
    def load_tree(self, data: Dict[str, Any]) -> None:
        """从字典数据加载行为树"""
        from .serializer import Serializer
        self.root_node = Serializer.deserialize(data)
    
    def load_from_file(self, filepath: str) -> None:
        """从文件加载行为树"""
        from .serializer import Serializer
        self.root_node = Serializer.load_from_file(filepath)
    
    def save_to_file(self, filepath: str, format: str = "json") -> None:
        """保存行为树到文件"""
        from .serializer import Serializer
        Serializer.save_to_file(self.root_node, filepath, format)
    
    def start(self, context: ExecutionContext = None) -> None:
        """启动执行"""
        if self._running:
            return
        
        self.context = context or ExecutionContext()
        self._running = True
        self._paused = False
        
        self._thread = threading.Thread(target=self._run_loop, daemon=True)
        self._thread.start()
        
        if self._on_status_change:
            self._on_status_change("running")
    
    def stop(self) -> None:
        """停止执行"""
        self._running = False
        self._paused = False
        
        if self.root_node:
            self.root_node.reset()
        
        if self._on_status_change:
            self._on_status_change("stopped")
    
    def pause(self) -> None:
        """暂停执行"""
        self._paused = True
        if self._on_status_change:
            self._on_status_change("paused")
    
    def resume(self) -> None:
        """恢复执行"""
        self._paused = False
        if self._on_status_change:
            self._on_status_change("running")
    
    def get_status(self) -> Dict[str, Any]:
        """获取执行状态"""
        return {
            "running": self._running,
            "paused": self._paused,
            "elapsed_time": self.context.elapsed_time if self.context else 0,
            "tick_count": self.context.tick_count if self.context else 0,
        }
    
    def _run_loop(self) -> None:
        """执行循环"""
        start_time = time.time()
        
        while self._running:
            if self._paused:
                time.sleep(0.1)
                continue
            
            # 更新上下文
            self.context.elapsed_time = time.time() - start_time
            self.context.tick_count += 1
            
            # 执行tick
            if self.root_node:
                status = self.root_node.tick(self.context)
                
                # 检查是否完成
                if status != NodeStatus.RUNNING:
                    self._running = False
                    if self._on_status_change:
                        self._on_status_change("completed", status)
            
            # 等待下一个tick
            time.sleep(self._tick_interval)
```

---

### 4.3 执行上下文 (bt_core/context.py)

```python
import time
from typing import Any, Callable, Optional

class ExecutionContext:
    """执行上下文"""
    
    def __init__(self):
        self.blackboard = Blackboard()
        self.elapsed_time: float = 0.0
        self.tick_count: int = 0
        self._is_running = True
        self._is_paused = False
        
        # 回调函数
        self._on_node_status: Optional[Callable] = None
        
        # 工具实例（延迟初始化）
        self._screenshot_manager = None
        self._input_controller = None
        self._ocr_manager = None
    
    @property
    def is_running(self) -> bool:
        return self._is_running
    
    @property
    def is_paused(self) -> bool:
        return self._is_paused
    
    def notify_node_status(self, node_id: str, status: str) -> None:
        """通知节点状态变化"""
        if self._on_node_status:
            self._on_node_status(node_id, status)
    
    def get_screenshot(self, region: tuple = None):
        """获取截图"""
        if self._screenshot_manager is None:
            from bt_utils.screenshot import ScreenshotManager
            self._screenshot_manager = ScreenshotManager()
        
        if region:
            return self._screenshot_manager.get_region_screenshot(region)
        return self._screenshot_manager.get_full_screenshot()
    
    def execute_key_press(self, key: str, action: str = "press", duration: int = 0) -> None:
        """执行按键操作"""
        if self._input_controller is None:
            from bt_utils.input_controller import InputController
            self._input_controller = InputController()
        
        self._input_controller.key_press(key, action, duration)
    
    def execute_mouse_click(self, button: str = "left", position: tuple = None, 
                           action: str = "press", duration: int = 0) -> None:
        """执行鼠标点击"""
        if self._input_controller is None:
            from bt_utils.input_controller import InputController
            self._input_controller = InputController()
        
        self._input_controller.mouse_click(button, position, action, duration)
    
    def execute_mouse_move(self, position: tuple, relative: bool = False) -> None:
        """执行鼠标移动"""
        if self._input_controller is None:
            from bt_utils.input_controller import InputController
            self._input_controller = InputController()
        
        self._input_controller.mouse_move(position, relative)
    
    def perform_ocr(self, image, keywords: str, language: str = "eng") -> tuple:
        """执行OCR识别"""
        if self._ocr_manager is None:
            from bt_utils.ocr_manager import OCRManager
            self._ocr_manager = OCRManager()
        
        return self._ocr_manager.recognize(image, keywords, language)
    
    def play_alarm(self, sound_path: str = None, volume: int = None, 
                   wait_complete: bool = True) -> None:
        """播放报警音"""
        from bt_utils.alarm import AlarmPlayer
        player = AlarmPlayer()
        player.play(sound_path, volume, wait_complete)
```

---

### 4.4 黑板系统 (bt_core/blackboard.py)

```python
from typing import Any, Dict, List, Callable, Optional

class Blackboard:
    """黑板系统 - 节点间数据共享"""
    
    BUILTIN_VARS = {
        "last_detection_position": None,  # 最后检测位置
        "last_number_value": None,        # 最后识别的数字值
        "execution_count": 0,             # 执行计数
    }
    
    def __init__(self):
        self._data: Dict[str, Any] = dict(self.BUILTIN_VARS)
        self._subscribers: Dict[str, List[Callable]] = {}
    
    def get(self, key: str, default: Any = None) -> Any:
        """获取变量值"""
        return self._data.get(key, default)
    
    def set(self, key: str, value: Any) -> None:
        """设置变量值"""
        old_value = self._data.get(key)
        self._data[key] = value
        self._notify_subscribers(key, old_value, value)
    
    def increment(self, key: str, amount: int = 1) -> None:
        """递增变量"""
        current = self.get(key, 0)
        if isinstance(current, (int, float)):
            self.set(key, current + amount)
    
    def delete(self, key: str) -> None:
        """删除变量"""
        if key in self._data and key not in self.BUILTIN_VARS:
            del self._data[key]
    
    def exists(self, key: str) -> bool:
        """检查变量是否存在"""
        return key in self._data
    
    def clear(self) -> None:
        """清空黑板（保留内置变量）"""
        self._data = dict(self.BUILTIN_VARS)
    
    def subscribe(self, key: str, callback: Callable[[Any, Any], None]) -> None:
        """订阅变量变化"""
        if key not in self._subscribers:
            self._subscribers[key] = []
        self._subscribers[key].append(callback)
    
    def unsubscribe(self, key: str, callback: Callable = None) -> None:
        """取消订阅"""
        if key not in self._subscribers:
            return
        
        if callback is None:
            del self._subscribers[key]
        elif callback in self._subscribers[key]:
            self._subscribers[key].remove(callback)
    
    def _notify_subscribers(self, key: str, old_value: Any, new_value: Any) -> None:
        """通知订阅者"""
        if key in self._subscribers:
            for callback in self._subscribers[key]:
                try:
                    callback(old_value, new_value)
                except Exception:
                    pass
    
    def to_dict(self) -> Dict[str, Any]:
        """导出为字典"""
        return dict(self._data)
    
    def from_dict(self, data: Dict[str, Any]) -> None:
        """从字典导入"""
        self._data.update(data)
```

---

### 4.5 序列化器 (bt_core/serializer.py)

```python
import json
import os
from typing import Dict, Any
from datetime import datetime

class Serializer:
    """行为树序列化器"""
    
    @staticmethod
    def serialize(root_node: "Node", metadata: Dict[str, Any] = None) -> Dict[str, Any]:
        """序列化行为树为字典"""
        nodes = {}
        connections = []
        
        def collect_nodes(node: "Node"):
            nodes[node.node_id] = node.to_dict()
            for child in node.children:
                connections.append({
                    "parent_id": node.node_id,
                    "child_id": child.node_id
                })
                collect_nodes(child)
        
        if root_node:
            collect_nodes(root_node)
        
        return {
            "version": "2.0",
            "format_type": "behavior_tree_standalone",
            "metadata": metadata or {
                "created_at": datetime.now().isoformat(),
                "modified_at": datetime.now().isoformat(),
                "app_version": "1.0.0",
            },
            "root_node": root_node.node_id if root_node else None,
            "nodes": nodes,
            "connections": connections,
        }
    
    @staticmethod
    def deserialize(data: Dict[str, Any]) -> "Node":
        """从字典反序列化行为树"""
        from .registry import NodeRegistry
        
        nodes_data = data.get("nodes", {})
        connections = data.get("connections", [])
        root_id = data.get("root_node")
        
        if not root_id or not nodes_data:
            return None
        
        # 创建所有节点
        nodes = {}
        for node_id, node_data in nodes_data.items():
            node = NodeRegistry.create_node(node_data)
            if node:
                nodes[node_id] = node
        
        # 建立连接关系
        for conn in connections:
            parent_id = conn["parent_id"]
            child_id = conn["child_id"]
            
            if parent_id in nodes and child_id in nodes:
                nodes[parent_id].add_child(nodes[child_id])
        
        return nodes.get(root_id)
    
    @staticmethod
    def save_to_file(root_node: "Node", filepath: str, format: str = "json") -> None:
        """保存到文件"""
        data = Serializer.serialize(root_node)
        
        if format == "json":
            with open(filepath, 'w', encoding='utf-8') as f:
                json.dump(data, f, indent=2, ensure_ascii=False)
        elif format in ("yaml", "yml"):
            try:
                import yaml
                with open(filepath, 'w', encoding='utf-8') as f:
                    yaml.dump(data, f, allow_unicode=True, default_flow_style=False)
            except ImportError:
                raise ImportError("PyYAML is required for YAML format")
        elif format in ("txt", "bt"):
            text = Serializer._to_text_format(root_node)
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(text)
    
    @staticmethod
    def load_from_file(filepath: str) -> "Node":
        """从文件加载"""
        ext = os.path.splitext(filepath)[1].lower()
        
        if ext == ".json":
            with open(filepath, 'r', encoding='utf-8') as f:
                data = json.load(f)
            return Serializer.deserialize(data)
        elif ext in (".yaml", ".yml"):
            try:
                import yaml
                with open(filepath, 'r', encoding='utf-8') as f:
                    data = yaml.safe_load(f)
                return Serializer.deserialize(data)
            except ImportError:
                raise ImportError("PyYAML is required for YAML format")
        elif ext in (".txt", ".bt"):
            with open(filepath, 'r', encoding='utf-8') as f:
                text = f.read()
            return Serializer._from_text_format(text)
    
    @staticmethod
    def _to_text_format(node: "Node", indent: int = 0) -> str:
        """转换为文本格式"""
        pass
    
    @staticmethod
    def _from_text_format(text: str) -> "Node":
        """从文本格式解析"""
        pass
```

---

### 4.6 节点注册中心 (bt_core/registry.py)

```python
from typing import Dict, Type, Any, Optional

class NodeRegistry:
    """节点类型注册中心"""
    
    _node_types: Dict[str, Type["Node"]] = {}
    
    @classmethod
    def register(cls, node_type: str, node_class: Type["Node"]) -> None:
        """注册节点类型"""
        cls._node_types[node_type] = node_class
    
    @classmethod
    def unregister(cls, node_type: str) -> None:
        """注销节点类型"""
        if node_type in cls._node_types:
            del cls._node_types[node_type]
    
    @classmethod
    def get(cls, node_type: str) -> Optional[Type["Node"]]:
        """获取节点类"""
        return cls._node_types.get(node_type)
    
    @classmethod
    def create_node(cls, data: Dict[str, Any]) -> Optional["Node"]:
        """创建节点实例"""
        node_type = data.get("type", "")
        node_class = cls.get(node_type)
        
        if node_class is None:
            return None
        
        return node_class.from_dict(data)
    
    @classmethod
    def list_types(cls) -> Dict[str, Type["Node"]]:
        """列出所有注册的节点类型"""
        return dict(cls._node_types)


def register_core_nodes():
    """注册核心节点类型"""
    from .nodes import SequenceNode, SelectorNode, ParallelNode
    from .nodes import ConditionNode, ActionNode
    
    NodeRegistry.register("SequenceNode", SequenceNode)
    NodeRegistry.register("SelectorNode", SelectorNode)
    NodeRegistry.register("ParallelNode", ParallelNode)
    NodeRegistry.register("ConditionNode", ConditionNode)
    NodeRegistry.register("ActionNode", ActionNode)
```

---

## 5. 节点扩展设计

### 5.1 动作节点

| 节点类型 | 类名 | 功能说明 |
|---------|------|---------|
| 按键节点 | KeyPressNode | 模拟键盘按键 |
| 鼠标点击节点 | MouseClickNode | 模拟鼠标点击 |
| 鼠标移动节点 | MouseMoveNode | 移动鼠标位置 |
| 鼠标滚轮节点 | MouseScrollNode | 模拟鼠标滚轮 |
| 延时节点 | DelayNode | 等待指定时间 |
| 设置变量节点 | SetVariableNode | 设置黑板变量 |
| 脚本节点 | ScriptNode | 执行脚本文件 |
| 代码节点 | CodeNode | 执行代码文件 |
| 报警节点 | AlarmNode | 播放报警音效 |

### 5.2 条件节点

| 节点类型 | 类名 | 功能说明 |
|---------|------|---------|
| OCR检测节点 | OCRConditionNode | OCR文字检测 |
| 图像检测节点 | ImageConditionNode | 图像模板匹配 |
| 颜色检测节点 | ColorConditionNode | 颜色检测 |
| 数字检测节点 | NumberConditionNode | 数字识别与比较 |
| 变量判断节点 | VariableConditionNode | 黑板变量判断 |

---

## 6. GUI设计

### 6.1 主界面布局

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              工具栏                                     │
│  [新建] [打开] [保存] │ [撤销] [重做] │ [运行] [停止]    状态: 就绪      │
├────────────┬───────────────────────────────────────────┬────────────────┤
│            │                                           │                │
│   节点面板  │                                           │    属性面板    │
│            │                                           │                │
│  ▼ 组合节点 │                                           │   节点属性     │
│   ◇ 顺序   │                                           │   ─────────   │
│   ◇ 选择   │              画布区域                      │   名称: ___   │
│   ◇ 并行   │                                           │   启用: [✓]   │
│            │                                           │                │
│  ▼ 条件节点 │                                           │   配置参数     │
│   ◇ OCR检测│                                           │   ─────────   │
│   ◇ 图像匹配│                                           │   区域: ___   │
│   ◇ 颜色检测│                                           │   关键词: ___ │
│   ◇ 数字比较│                                           │                │
│   ◇ 变量判断│                                           │   装饰参数     │
│            │                                           │   ─────────   │
│  ▼ 动作节点 │                                           │   重试: 0     │
│   ◆ 按键   │                                           │   超时: 0     │
│   ◆ 点击   │                                           │                │
│   ◆ 移动   │                                           │                │
│   ◆ 延时   │                                           │                │
│   ◆ 设变量 │                                           │                │
│   ◆ 代码   │                                           │                │
│   ◆ 脚本   │                                           │                │
│   ◆ 报警   │                                           │                │
│            │                                           │                │
└────────────┴───────────────────────────────────────────┴────────────────┘
```

### 6.2 主要功能

- **节点拖拽**：从节点面板拖拽节点到画布
- **连线操作**：拖拽端口建立节点连接
- **属性编辑**：选中节点后编辑属性
- **撤销/重做**：支持操作历史管理
- **运行控制**：运行、暂停、停止行为树
- **状态显示**：实时显示节点执行状态

---

## 7. 脚本录制界面设计

### 7.1 界面布局

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           脚本录制编辑器                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────┐  ┌─────────────────────────────────────┐  │
│  │      命令面板           │  │           脚本编辑区                │  │
│  │                         │  │                                     │  │
│  │  ┌───────────────────┐  │  │  ┌─────────────────────────────┐   │  │
│  │  │ 按键命令          │  │  │  │                             │   │  │
│  │  │ 按键: [___] [修改]│  │  │  │  KeyDown "space", 1         │   │  │
│  │  │ 类型: [KeyDown ▼]│  │  │  │  Delay 500                  │   │  │
│  │  │       [插入]      │  │  │  │  KeyUp "space", 1           │   │  │
│  │  └───────────────────┘  │  │  │  MoveTo 100, 200            │   │  │
│  │                         │  │  │  LeftDown 1                 │   │  │
│  │  ┌───────────────────┐  │  │  │  Delay 100                  │   │  │
│  │  │ 延迟命令          │  │  │  │  LeftUp 1                   │   │  │
│  │  │ 延迟(ms): [_____]│  │  │  │  ...                        │   │  │
│  │  │         [插入]    │  │  │  │                             │   │  │
│  │  └───────────────────┘  │  │  └─────────────────────────────┘   │  │
│  │                         │  │                                     │  │
│  │  ┌───────────────────┐  │  │  当前文件: script.txt              │  │
│  │  │ 鼠标命令          │  │  │                                     │  │
│  │  │ 按键: [Left  ▼]   │  │  └─────────────────────────────────────┘  │
│  │  │ 操作: [Down  ▼]   │  │                                             │
│  │  │       [插入]      │  │  ┌─────────────────────────────────────┐  │
│  │  │ [选择坐标点]      │  │  │           脚本控制                  │  │
│  │  └───────────────────┘  │  │                                     │  │
│  │                         │  │  [开始录制] [停止录制]              │  │
│  │  ┌───────────────────┐  │  │  [新建] [打开] [保存] [清空]        │  │
│  │  │ 组合按键          │  │  │                                     │  │
│  │  │ 按键: [___] [修改]│  │  └─────────────────────────────────────┘  │
│  │  │ 按键延迟: [_____]│  │                                             │
│  │  │ 抬起延迟: [_____]│  │                                             │
│  │  │       [插入]      │  │                                             │
│  │  └───────────────────┘  │                                             │
│  │                         │                                             │
│  └─────────────────────────┘                                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 7.2 功能模块

#### 7.2.1 按键命令面板

| 组件 | 说明 |
|------|------|
| 按键输入框 | 显示当前选择的按键，禁用状态，通过监听获取 |
| 修改按钮 | 点击后开始监听键盘，捕获下一个按键 |
| 类型下拉框 | 选择 KeyDown 或 KeyUp |
| 插入按钮 | 将命令插入到脚本编辑区 |

**脚本格式**：
```
KeyDown "key", count
KeyUp "key", count
```

#### 7.2.2 延迟命令面板

| 组件 | 说明 |
|------|------|
| 延迟输入框 | 输入延迟时间（毫秒） |
| 插入按钮 | 将延迟命令插入到脚本编辑区 |

**脚本格式**：
```
Delay milliseconds
```

#### 7.2.3 鼠标命令面板

| 组件 | 说明 |
|------|------|
| 按键下拉框 | 选择 Left/Right/Middle |
| 操作下拉框 | 选择 Down/Up |
| 插入按钮 | 将命令插入到脚本编辑区 |
| 选择坐标点按钮 | 打开全屏坐标选择窗口 |

**脚本格式**：
```
LeftDown count
LeftUp count
RightDown count
RightUp count
MiddleDown count
MiddleUp count
MoveTo x, y
```

#### 7.2.4 组合按键面板

| 组件 | 说明 |
|------|------|
| 按键输入框 | 显示当前选择的按键 |
| 修改按钮 | 点击后开始监听键盘 |
| 按键延迟 | 按下后等待的时间（毫秒） |
| 抬起延迟 | 抬起后等待的时间（毫秒） |
| 插入按钮 | 生成完整的组合按键序列 |

**生成的脚本**：
```
Delay key_delay
KeyDown "key", 1
Delay after_delay
KeyUp "key", 1
```

#### 7.2.5 脚本控制面板

| 按钮 | 功能 |
|------|------|
| 开始录制 | 开始录制键盘和鼠标操作 |
| 停止录制 | 停止录制并生成脚本 |
| 新建 | 清空当前脚本，新建空白脚本 |
| 打开 | 打开脚本文件 |
| 保存 | 保存当前脚本 |
| 清空 | 清空脚本编辑区 |

### 7.3 录制功能实现

```python
class ScriptRecorder:
    """脚本录制器"""
    
    def __init__(self, app):
        self.app = app
        self.is_recording = False
        self.events = []
        self.start_time = None
        self.last_event_time = None
        self.pressed_keys = set()
        self.last_mouse_position = None
        
        self.keyboard_listener = None
        self.mouse_listener = None
    
    def start_recording(self):
        """开始录制"""
        self.is_recording = True
        self.events = []
        self.start_time = time.time()
        self.last_event_time = self.start_time
        
        from pynput import keyboard, mouse
        
        self.keyboard_listener = keyboard.Listener(
            on_press=self._on_key_press,
            on_release=self._on_key_release
        )
        self.mouse_listener = mouse.Listener(
            on_move=self._on_mouse_move,
            on_click=self._on_mouse_click
        )
        
        self.keyboard_listener.start()
        self.mouse_listener.start()
    
    def stop_recording(self):
        """停止录制"""
        self.is_recording = False
        
        if self.keyboard_listener:
            self.keyboard_listener.stop()
        if self.mouse_listener:
            self.mouse_listener.stop()
        
        self._generate_script()
    
    def _on_key_press(self, key):
        """按键按下事件"""
        if not self.is_recording:
            return
        
        key_name = self._get_key_name(key)
        if key_name and key_name not in self.pressed_keys:
            self._add_delay()
            self.events.append({
                "type": "keydown",
                "key": key_name
            })
            self.pressed_keys.add(key_name)
    
    def _on_key_release(self, key):
        """按键释放事件"""
        if not self.is_recording:
            return
        
        key_name = self._get_key_name(key)
        if key_name and key_name in self.pressed_keys:
            self._add_delay()
            self.events.append({
                "type": "keyup",
                "key": key_name
            })
            self.pressed_keys.remove(key_name)
    
    def _on_mouse_move(self, x, y):
        """鼠标移动事件"""
        self.last_mouse_position = (x, y)
    
    def _on_mouse_click(self, x, y, button, pressed):
        """鼠标点击事件"""
        if not self.is_recording:
            return
        
        self._add_delay()
        
        if self.last_mouse_position:
            self.events.append({
                "type": "moveto",
                "x": self.last_mouse_position[0],
                "y": self.last_mouse_position[1]
            })
        
        self.events.append({
            "type": f"mouse_{'down' if pressed else 'up'}",
            "button": button.name
        })
    
    def _add_delay(self):
        """添加延迟事件"""
        current_time = time.time()
        delay = int((current_time - self.last_event_time) * 1000)
        if delay > 0:
            self.events.append({
                "type": "delay",
                "time": delay
            })
        self.last_event_time = current_time
    
    def _generate_script(self):
        """生成脚本"""
        script = ""
        for event in self.events:
            if event["type"] == "delay":
                script += f"Delay {event['time']}\n"
            elif event["type"] == "keydown":
                script += f'KeyDown "{event['key']}", 1\n'
            elif event["type"] == "keyup":
                script += f'KeyUp "{event['key']}", 1\n'
            elif event["type"] == "moveto":
                script += f"MoveTo {event['x']}, {event['y']}\n"
            elif event["type"] == "mouse_down":
                button = event["button"].capitalize()
                script += f"{button}Down 1\n"
            elif event["type"] == "mouse_up":
                button = event["button"].capitalize()
                script += f"{button}Up 1\n"
        
        self.app.insert_script(script)
```

### 7.4 脚本执行器

```python
class ScriptExecutor:
    """脚本执行器"""
    
    def __init__(self, app):
        self.app = app
        self.is_running = False
        self.is_paused = False
        self.execution_thread = None
    
    def run_script(self, script_content, loop=False):
        """执行脚本"""
        commands = self._parse_script(script_content)
        if not commands:
            return
        
        self.is_running = True
        self.is_paused = False
        
        def execute():
            pressed_keys = set()
            
            while self.is_running:
                for command in commands:
                    if not self.is_running:
                        break
                    
                    while self.is_paused:
                        time.sleep(0.1)
                    
                    self._execute_command(command, pressed_keys)
                
                if not loop:
                    break
            
            self._release_all_keys(pressed_keys)
            self.is_running = False
        
        self.execution_thread = threading.Thread(target=execute, daemon=True)
        self.execution_thread.start()
    
    def _parse_script(self, content):
        """解析脚本"""
        import re
        commands = []
        
        for line in content.splitlines():
            line = line.strip()
            if not line:
                continue
            
            # KeyDown/KeyUp "key", count
            key_pattern = re.compile(r'^(KeyDown|KeyUp)\s+["\'](.*?)["\']\s*,\s*(\d+)$', re.IGNORECASE)
            match = key_pattern.match(line)
            if match:
                commands.append({
                    "type": match.group(1).lower(),
                    "key": match.group(2).lower(),
                    "count": int(match.group(3))
                })
                continue
            
            # Mouse Down/Up
            mouse_pattern = re.compile(r'^(Left|Right|Middle)(Down|Up)\s+(\d+)$', re.IGNORECASE)
            match = mouse_pattern.match(line)
            if match:
                commands.append({
                    "type": f"mouse_{match.group(2).lower()}",
                    "button": match.group(1).lower(),
                    "count": int(match.group(3))
                })
                continue
            
            # MoveTo x, y
            move_pattern = re.compile(r'^MoveTo\s+(\d+)\s*,\s*(\d+)$', re.IGNORECASE)
            match = move_pattern.match(line)
            if match:
                commands.append({
                    "type": "moveto",
                    "x": int(match.group(1)),
                    "y": int(match.group(2))
                })
                continue
            
            # Delay milliseconds
            delay_pattern = re.compile(r'^Delay\s+(\d+)$', re.IGNORECASE)
            match = delay_pattern.match(line)
            if match:
                commands.append({
                    "type": "delay",
                    "time": int(match.group(1))
                })
                continue
        
        return commands
    
    def _execute_command(self, command, pressed_keys):
        """执行单个命令"""
        if command["type"] == "keydown":
            key = command["key"]
            for _ in range(command["count"]):
                if key not in pressed_keys:
                    self.app.input_controller.key_down(key)
                    pressed_keys.add(key)
        
        elif command["type"] == "keyup":
            key = command["key"]
            for _ in range(command["count"]):
                if key in pressed_keys:
                    self.app.input_controller.key_up(key)
                    pressed_keys.remove(key)
        
        elif command["type"] == "mouse_down":
            for _ in range(command["count"]):
                self.app.input_controller.mouse_down(command["button"])
        
        elif command["type"] == "mouse_up":
            for _ in range(command["count"]):
                self.app.input_controller.mouse_up(command["button"])
        
        elif command["type"] == "moveto":
            self.app.input_controller.move_to(command["x"], command["y"])
        
        elif command["type"] == "delay":
            time.sleep(command["time"] / 1000)
    
    def _release_all_keys(self, pressed_keys):
        """释放所有按键"""
        for key in pressed_keys:
            try:
                self.app.input_controller.key_up(key)
            except:
                pass
    
    def stop_script(self):
        """停止脚本"""
        self.is_running = False
        self.is_paused = False
    
    def pause_script(self):
        """暂停脚本"""
        self.is_paused = True
    
    def resume_script(self):
        """恢复脚本"""
        self.is_paused = False
```

---

## 8. 设置界面设计

### 8.1 界面布局

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              设置界面                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │ Tesseract OCR 设置                                                 │ │
│  │ ────────────────────────────────────────────────────────────────  │ │
│  │ 路径: [C:\Program Files\Tesseract-OCR\          ] [浏览]         │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │ 报警设置                                                           │ │
│  │ ────────────────────────────────────────────────────────────────  │ │
│  │ 声音: [alarm.mp3                                 ] [浏览]         │ │
│  │ 音量: ═════════════════════════════════○═══════════  70%          │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │ 快捷键设置                                                         │ │
│  │ ────────────────────────────────────────────────────────────────  │ │
│  │ 开始运行: [F10     ] [修改]                                        │ │
│  │ 停止运行: [F12     ] [修改]                                        │ │
│  │ 录制按钮: [F11     ] [修改]                                        │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │ 行为树设置                                                         │ │
│  │ ────────────────────────────────────────────────────────────────  │ │
│  │ Tick间隔(ms): [50    ]                                             │ │
│  │ 自动保存间隔(秒): [30  ]                                           │ │
│  │ 默认保存格式: [JSON ▼]                                             │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │ 界面设置                                                           │ │
│  │ ────────────────────────────────────────────────────────────────  │ │
│  │ 主题: [深色 ▼]                                                     │ │
│  │ 语言: [简体中文 ▼]                                                 │ │
│  │ 字体大小: [10 ▼]                                                   │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 8.2 设置项说明

#### 8.2.1 Tesseract OCR 设置

| 设置项 | 类型 | 说明 |
|--------|------|------|
| 路径 | string | Tesseract OCR 安装路径 |

**功能**：
- 设置 Tesseract OCR 的安装路径
- 自动检测路径是否有效
- 支持浏览选择路径

#### 8.2.2 报警设置

| 设置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| 声音 | string | "" | 报警音效文件路径 |
| 音量 | int | 70 | 音量百分比 (0-100) |

**功能**：
- 选择自定义报警音效
- 调节音量大小
- 支持试听功能

#### 8.2.3 快捷键设置

| 设置项 | 默认值 | 说明 |
|--------|--------|------|
| 开始运行 | F10 | 开始执行行为树 |
| 停止运行 | F12 | 停止执行行为树 |
| 录制按钮 | F11 | 开始/停止录制脚本 |

**功能**：
- 点击"修改"按钮后监听键盘
- 捕获下一个按键作为快捷键
- 支持组合键（如 Ctrl+F10）

#### 8.2.4 行为树设置

| 设置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| Tick间隔 | int | 50 | 行为树执行间隔（毫秒） |
| 自动保存间隔 | int | 30 | 自动保存间隔（秒） |
| 默认保存格式 | string | "json" | 默认文件格式 |

#### 8.2.5 界面设置

| 设置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| 主题 | string | "深色" | 界面主题 |
| 语言 | string | "简体中文" | 界面语言 |
| 字体大小 | int | 10 | 字体大小 |

### 8.3 设置管理器实现

```python
import json
import os
from typing import Any, Dict

class SettingsManager:
    """设置管理器"""
    
    DEFAULT_SETTINGS = {
        "tesseract_path": "",
        "alarm_sound_path": "",
        "alarm_volume": 70,
        "shortcuts": {
            "start": "F10",
            "stop": "F12",
            "record": "F11"
        },
        "behavior_tree": {
            "tick_interval": 50,
            "auto_save_interval": 30,
            "default_format": "json"
        },
        "ui": {
            "theme": "dark",
            "language": "zh_CN",
            "font_size": 10
        }
    }
    
    def __init__(self, config_dir: str = None):
        if config_dir is None:
            config_dir = self._get_default_config_dir()
        
        self.config_dir = config_dir
        self.config_file = os.path.join(config_dir, "settings.json")
        self.settings: Dict[str, Any] = {}
        
        self._load_settings()
    
    def _get_default_config_dir(self) -> str:
        """获取默认配置目录"""
        if os.name == 'nt':
            base_dir = os.environ.get('APPDATA', os.path.expanduser('~'))
        else:
            base_dir = os.environ.get('XDG_CONFIG_HOME', 
                                       os.path.join(os.path.expanduser('~'), '.config'))
        
        return os.path.join(base_dir, "behavior_tree_standalone")
    
    def _load_settings(self) -> None:
        """加载设置"""
        if os.path.exists(self.config_file):
            try:
                with open(self.config_file, 'r', encoding='utf-8') as f:
                    self.settings = json.load(f)
                self._merge_defaults()
            except Exception:
                self.settings = dict(self.DEFAULT_SETTINGS)
        else:
            self.settings = dict(self.DEFAULT_SETTINGS)
    
    def _merge_defaults(self) -> None:
        """合并默认设置"""
        def merge_dict(base: dict, default: dict) -> dict:
            for key, value in default.items():
                if key not in base:
                    base[key] = value
                elif isinstance(value, dict):
                    merge_dict(base[key], value)
            return base
        
        merge_dict(self.settings, self.DEFAULT_SETTINGS)
    
    def save_settings(self) -> None:
        """保存设置"""
        os.makedirs(self.config_dir, exist_ok=True)
        
        with open(self.config_file, 'w', encoding='utf-8') as f:
            json.dump(self.settings, f, indent=2, ensure_ascii=False)
    
    def get(self, key: str, default: Any = None) -> Any:
        """获取设置值"""
        keys = key.split('.')
        value = self.settings
        
        for k in keys:
            if isinstance(value, dict) and k in value:
                value = value[k]
            else:
                return default
        
        return value
    
    def set(self, key: str, value: Any) -> None:
        """设置值"""
        keys = key.split('.')
        data = self.settings
        
        for k in keys[:-1]:
            if k not in data:
                data[k] = {}
            data = data[k]
        
        data[keys[-1]] = value
    
    def reset(self, key: str = None) -> None:
        """重置设置"""
        if key is None:
            self.settings = dict(self.DEFAULT_SETTINGS)
        else:
            keys = key.split('.')
            default_value = self.DEFAULT_SETTINGS
            
            for k in keys:
                if isinstance(default_value, dict) and k in default_value:
                    default_value = default_value[k]
                else:
                    return
            
            self.set(key, default_value)
```

### 8.4 设置界面实现

```python
import customtkinter as ctk
import tkinter as tk
from tkinter import filedialog, messagebox

class SettingsDialog:
    """设置对话框"""
    
    def __init__(self, parent, settings_manager: SettingsManager):
        self.parent = parent
        self.settings = settings_manager
        
        self.dialog = ctk.CTkToplevel(parent)
        self.dialog.title("设置")
        self.dialog.geometry("600x500")
        self.dialog.transient(parent)
        self.dialog.grab_set()
        
        self._create_widgets()
        self._load_values()
    
    def _create_widgets(self):
        """创建界面组件"""
        scroll_frame = ctk.CTkScrollableFrame(self.dialog)
        scroll_frame.pack(fill='both', expand=True, padx=10, pady=10)
        
        # Tesseract OCR 设置
        self._create_tesseract_section(scroll_frame)
        
        # 报警设置
        self._create_alarm_section(scroll_frame)
        
        # 快捷键设置
        self._create_shortcuts_section(scroll_frame)
        
        # 行为树设置
        self._create_behavior_tree_section(scroll_frame)
        
        # 界面设置
        self._create_ui_section(scroll_frame)
        
        # 按钮
        btn_frame = ctk.CTkFrame(self.dialog, fg_color='transparent')
        btn_frame.pack(fill='x', padx=10, pady=10)
        
        ctk.CTkButton(btn_frame, text="保存", command=self._save).pack(side='right', padx=5)
        ctk.CTkButton(btn_frame, text="取消", command=self.dialog.destroy).pack(side='right', padx=5)
        ctk.CTkButton(btn_frame, text="重置默认", command=self._reset).pack(side='left', padx=5)
    
    def _create_tesseract_section(self, parent):
        """创建 Tesseract OCR 设置区域"""
        frame = ctk.CTkFrame(parent)
        frame.pack(fill='x', pady=5)
        
        ctk.CTkLabel(frame, text="Tesseract OCR 设置", font=('', 14, 'bold')).pack(anchor='w', padx=10, pady=5)
        
        row = ctk.CTkFrame(frame, fg_color='transparent')
        row.pack(fill='x', padx=10, pady=5)
        
        ctk.CTkLabel(row, text="路径:").pack(side='left')
        self.tesseract_path = ctk.CTkEntry(row, width=300)
        self.tesseract_path.pack(side='left', padx=5, fill='x', expand=True)
        ctk.CTkButton(row, text="浏览", width=60, command=self._browse_tesseract).pack(side='left')
    
    def _create_alarm_section(self, parent):
        """创建报警设置区域"""
        frame = ctk.CTkFrame(parent)
        frame.pack(fill='x', pady=5)
        
        ctk.CTkLabel(frame, text="报警设置", font=('', 14, 'bold')).pack(anchor='w', padx=10, pady=5)
        
        # 声音文件
        row1 = ctk.CTkFrame(frame, fg_color='transparent')
        row1.pack(fill='x', padx=10, pady=5)
        
        ctk.CTkLabel(row1, text="声音:").pack(side='left')
        self.alarm_sound = ctk.CTkEntry(row1, width=300)
        self.alarm_sound.pack(side='left', padx=5, fill='x', expand=True)
        ctk.CTkButton(row1, text="浏览", width=60, command=self._browse_alarm).pack(side='left')
        
        # 音量
        row2 = ctk.CTkFrame(frame, fg_color='transparent')
        row2.pack(fill='x', padx=10, pady=5)
        
        ctk.CTkLabel(row2, text="音量:").pack(side='left')
        self.alarm_volume = ctk.CTkSlider(row2, from_=0, to=100, width=200)
        self.alarm_volume.pack(side='left', padx=5)
        self.volume_label = ctk.CTkLabel(row2, text="70%", width=40)
        self.volume_label.pack(side='left')
        
        self.alarm_volume.configure(command=self._update_volume_label)
    
    def _create_shortcuts_section(self, parent):
        """创建快捷键设置区域"""
        frame = ctk.CTkFrame(parent)
        frame.pack(fill='x', pady=5)
        
        ctk.CTkLabel(frame, text="快捷键设置", font=('', 14, 'bold')).pack(anchor='w', padx=10, pady=5)
        
        shortcuts = [
            ("开始运行:", "start_shortcut"),
            ("停止运行:", "stop_shortcut"),
            ("录制按钮:", "record_shortcut")
        ]
        
        self.shortcut_vars = {}
        
        for label, var_name in shortcuts:
            row = ctk.CTkFrame(frame, fg_color='transparent')
            row.pack(fill='x', padx=10, pady=2)
            
            ctk.CTkLabel(row, text=label, width=80).pack(side='left')
            
            var = tk.StringVar()
            self.shortcut_vars[var_name] = var
            
            entry = ctk.CTkEntry(row, textvariable=var, width=80, state='disabled')
            entry.pack(side='left', padx=5)
            
            ctk.CTkButton(row, text="修改", width=60,
                         command=lambda e=entry: self._listen_shortcut(e)).pack(side='left')
    
    def _create_behavior_tree_section(self, parent):
        """创建行为树设置区域"""
        frame = ctk.CTkFrame(parent)
        frame.pack(fill='x', pady=5)
        
        ctk.CTkLabel(frame, text="行为树设置", font=('', 14, 'bold')).pack(anchor='w', padx=10, pady=5)
        
        # Tick间隔
        row1 = ctk.CTkFrame(frame, fg_color='transparent')
        row1.pack(fill='x', padx=10, pady=5)
        
        ctk.CTkLabel(row1, text="Tick间隔(ms):").pack(side='left')
        self.tick_interval = ctk.CTkEntry(row1, width=60)
        self.tick_interval.pack(side='left', padx=5)
        
        # 自动保存间隔
        row2 = ctk.CTkFrame(frame, fg_color='transparent')
        row2.pack(fill='x', padx=10, pady=5)
        
        ctk.CTkLabel(row2, text="自动保存间隔(秒):").pack(side='left')
        self.auto_save_interval = ctk.CTkEntry(row2, width=60)
        self.auto_save_interval.pack(side='left', padx=5)
        
        # 默认格式
        row3 = ctk.CTkFrame(frame, fg_color='transparent')
        row3.pack(fill='x', padx=10, pady=5)
        
        ctk.CTkLabel(row3, text="默认保存格式:").pack(side='left')
        self.default_format = ctk.CTkComboBox(row3, values=["json", "yaml", "txt"], width=100)
        self.default_format.pack(side='left', padx=5)
    
    def _create_ui_section(self, parent):
        """创建界面设置区域"""
        frame = ctk.CTkFrame(parent)
        frame.pack(fill='x', pady=5)
        
        ctk.CTkLabel(frame, text="界面设置", font=('', 14, 'bold')).pack(anchor='w', padx=10, pady=5)
        
        # 主题
        row1 = ctk.CTkFrame(frame, fg_color='transparent')
        row1.pack(fill='x', padx=10, pady=5)
        
        ctk.CTkLabel(row1, text="主题:").pack(side='left')
        self.theme = ctk.CTkComboBox(row1, values=["深色", "浅色", "系统"], width=100)
        self.theme.pack(side='left', padx=5)
        
        # 语言
        row2 = ctk.CTkFrame(frame, fg_color='transparent')
        row2.pack(fill='x', padx=10, pady=5)
        
        ctk.CTkLabel(row2, text="语言:").pack(side='left')
        self.language = ctk.CTkComboBox(row2, values=["简体中文", "English"], width=100)
        self.language.pack(side='left', padx=5)
    
    def _load_values(self):
        """加载设置值"""
        self.tesseract_path.insert(0, self.settings.get("tesseract_path", ""))
        self.alarm_sound.insert(0, self.settings.get("alarm_sound_path", ""))
        self.alarm_volume.set(self.settings.get("alarm_volume", 70))
        
        shortcuts = self.settings.get("shortcuts", {})
        self.shortcut_vars["start_shortcut"].set(shortcuts.get("start", "F10"))
        self.shortcut_vars["stop_shortcut"].set(shortcuts.get("stop", "F12"))
        self.shortcut_vars["record_shortcut"].set(shortcuts.get("record", "F11"))
        
        bt_settings = self.settings.get("behavior_tree", {})
        self.tick_interval.insert(0, str(bt_settings.get("tick_interval", 50)))
        self.auto_save_interval.insert(0, str(bt_settings.get("auto_save_interval", 30)))
        self.default_format.set(bt_settings.get("default_format", "json"))
        
        ui_settings = self.settings.get("ui", {})
        self.theme.set(ui_settings.get("theme", "深色"))
        self.language.set(ui_settings.get("language", "简体中文"))
    
    def _update_volume_label(self, value):
        """更新音量标签"""
        self.volume_label.configure(text=f"{int(value)}%")
    
    def _browse_tesseract(self):
        """浏览 Tesseract 路径"""
        path = filedialog.askdirectory(title="选择 Tesseract OCR 目录")
        if path:
            self.tesseract_path.delete(0, 'end')
            self.tesseract_path.insert(0, path)
    
    def _browse_alarm(self):
        """浏览报警音效文件"""
        path = filedialog.askopenfilename(
            title="选择报警音效",
            filetypes=[("音频文件", "*.mp3 *.wav *.ogg"), ("所有文件", "*.*")]
        )
        if path:
            self.alarm_sound.delete(0, 'end')
            self.alarm_sound.insert(0, path)
    
    def _listen_shortcut(self, entry):
        """监听快捷键"""
        entry.configure(state='normal')
        entry.delete(0, 'end')
        entry.insert(0, "按下按键...")
        
        def on_key_press(key):
            try:
                key_name = key.name if hasattr(key, 'name') else str(key)
                entry.delete(0, 'end')
                entry.insert(0, key_name.upper())
                entry.configure(state='disabled')
                return False
            except:
                return True
        
        from pynput import keyboard
        listener = keyboard.Listener(on_press=on_key_press)
        listener.start()
    
    def _save(self):
        """保存设置"""
        self.settings.set("tesseract_path", self.tesseract_path.get())
        self.settings.set("alarm_sound_path", self.alarm_sound.get())
        self.settings.set("alarm_volume", int(self.alarm_volume.get()))
        
        self.settings.set("shortcuts", {
            "start": self.shortcut_vars["start_shortcut"].get(),
            "stop": self.shortcut_vars["stop_shortcut"].get(),
            "record": self.shortcut_vars["record_shortcut"].get()
        })
        
        self.settings.set("behavior_tree", {
            "tick_interval": int(self.tick_interval.get()),
            "auto_save_interval": int(self.auto_save_interval.get()),
            "default_format": self.default_format.get()
        })
        
        self.settings.set("ui", {
            "theme": self.theme.get(),
            "language": self.language.get()
        })
        
        self.settings.save_settings()
        messagebox.showinfo("成功", "设置已保存")
        self.dialog.destroy()
    
    def _reset(self):
        """重置设置"""
        if messagebox.askyesno("确认", "确定要重置所有设置为默认值吗？"):
            self.settings.reset()
            self._load_values()
```

---

## 9. 工具模块设计

### 9.1 截图管理器 (bt_utils/screenshot.py)

```python
from PIL import ImageGrab, Image
from typing import Optional, Tuple

class ScreenshotManager:
    """截图管理器"""
    
    _instance = None
    
    def __new__(cls):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
        return cls._instance
    
    def get_full_screenshot(self) -> Image.Image:
        """获取全屏截图"""
        return ImageGrab.grab()
    
    def get_region_screenshot(self, region: Tuple[int, int, int, int]) -> Image.Image:
        """获取区域截图"""
        return ImageGrab.grab(bbox=region)
```

### 9.2 输入控制器 (bt_utils/input_controller.py)

```python
import pyautogui
from typing import Tuple, Optional

class InputController:
    """输入控制器"""
    
    def __init__(self):
        pyautogui.FAILSAFE = True
        pyautogui.PAUSE = 0.01
    
    def key_press(self, key: str, action: str = "press", duration: int = 0) -> None:
        """按键操作"""
        if action == "press":
            if duration > 0:
                pyautogui.keyDown(key)
                pyautogui.sleep(duration / 1000)
                pyautogui.keyUp(key)
            else:
                pyautogui.press(key)
        elif action == "down":
            pyautogui.keyDown(key)
        elif action == "up":
            pyautogui.keyUp(key)
    
    def mouse_click(self, button: str = "left", position: Tuple[int, int] = None,
                   action: str = "press", duration: int = 0) -> None:
        """鼠标点击"""
        if position:
            pyautogui.moveTo(position[0], position[1])
        
        if action == "press":
            if duration > 0:
                pyautogui.mouseDown(button=button)
                pyautogui.sleep(duration / 1000)
                pyautogui.mouseUp(button=button)
            else:
                pyautogui.click(button=button)
        elif action == "down":
            pyautogui.mouseDown(button=button)
        elif action == "up":
            pyautogui.mouseUp(button=button)
    
    def mouse_move(self, position: Tuple[int, int], relative: bool = False) -> None:
        """鼠标移动"""
        if relative:
            pyautogui.move(position[0], position[1])
        else:
            pyautogui.moveTo(position[0], position[1])
    
    def mouse_scroll(self, amount: int, position: Tuple[int, int] = None) -> None:
        """鼠标滚轮"""
        if position:
            pyautogui.moveTo(position[0], position[1])
        pyautogui.scroll(amount)
```

### 9.3 脚本录制器 (bt_utils/recorder.py)

```python
import threading
import time
from typing import List, Callable
from pynput import mouse, keyboard

class ScriptRecorder:
    """脚本录制器"""
    
    def __init__(self):
        self._recording = False
        self._events: List[dict] = []
        self._start_time: float = 0
        self._mouse_listener = None
        self._keyboard_listener = None
        self._on_event: Callable = None
    
    def start_recording(self) -> None:
        """开始录制"""
        self._recording = True
        self._events = []
        self._start_time = time.time()
        
        self._mouse_listener = mouse.Listener(
            on_click=self._on_mouse_click,
            on_scroll=self._on_mouse_scroll
        )
        self._keyboard_listener = keyboard.Listener(
            on_press=self._on_key_press,
            on_release=self._on_key_release
        )
        
        self._mouse_listener.start()
        self._keyboard_listener.start()
    
    def stop_recording(self) -> List[dict]:
        """停止录制"""
        self._recording = False
        
        if self._mouse_listener:
            self._mouse_listener.stop()
        if self._keyboard_listener:
            self._keyboard_listener.stop()
        
        return self._events
    
    def save_to_file(self, filepath: str) -> None:
        """保存到脚本文件"""
        with open(filepath, 'w', encoding='utf-8') as f:
            for event in self._events:
                f.write(self._format_event(event) + '\n')
    
    def _on_mouse_click(self, x, y, button, pressed):
        if not self._recording:
            return
        
        event = {
            "type": "mouse_click",
            "time": time.time() - self._start_time,
            "x": x,
            "y": y,
            "button": str(button),
            "pressed": pressed
        }
        self._events.append(event)
        
        if self._on_event:
            self._on_event(event)
    
    def _on_mouse_scroll(self, x, y, dx, dy):
        if not self._recording:
            return
        
        event = {
            "type": "mouse_scroll",
            "time": time.time() - self._start_time,
            "x": x,
            "y": y,
            "dx": dx,
            "dy": dy
        }
        self._events.append(event)
        
        if self._on_event:
            self._on_event(event)
    
    def _on_key_press(self, key):
        if not self._recording:
            return
        
        event = {
            "type": "key_press",
            "time": time.time() - self._start_time,
            "key": str(key)
        }
        self._events.append(event)
        
        if self._on_event:
            self._on_event(event)
    
    def _on_key_release(self, key):
        if not self._recording:
            return
        
        event = {
            "type": "key_release",
            "time": time.time() - self._start_time,
            "key": str(key)
        }
        self._events.append(event)
    
    def _format_event(self, event: dict) -> str:
        """格式化事件为脚本格式"""
        pass
```

---

## 10. 依赖清单

### 10.1 requirements.txt

```txt
customtkinter>=5.2.0
Pillow>=10.0.0
pyautogui>=0.9.54
pytesseract>=0.3.10
pygame>=2.0.0
pynput>=1.7.6
opencv-python-headless>=4.8.0
numpy>=1.21.0
pywin32>=305
PyYAML>=6.0
```

### 10.2 依赖说明

| 依赖包 | 用途 | 必需性 |
|--------|------|--------|
| customtkinter | GUI框架 | GUI必需 |
| Pillow | 图像处理 | 必需 |
| pyautogui | 自动化操作 | 动作节点必需 |
| pytesseract | OCR识别 | OCR节点必需 |
| pygame | 音频播放 | 报警节点必需 |
| pynput | 键盘监听 | 录制功能必需 |
| opencv-python-headless | 图像检测 | 图像节点必需 |
| numpy | 数值计算 | 必需 |
| pywin32 | Windows API | Windows平台必需 |
| PyYAML | YAML格式支持 | 可选 |

---

## 11. 使用示例

### 11.1 作为库使用

```python
from bt_core import BehaviorTreeEngine, ExecutionContext
from bt_core.nodes import SequenceNode, SelectorNode
from bt_nodes.actions import KeyPressNode, DelayNode
from bt_nodes.conditions import OCRConditionNode

# 创建行为树
root = SequenceNode("root", NodeConfig(name="主流程"))

# 添加子节点
ocr_check = OCRConditionNode("ocr_1", NodeConfig(
    name="检测开始按钮",
    region=(100, 100, 300, 150),
    keywords="开始"
))

delay = DelayNode("delay_1", NodeConfig(
    name="等待1秒",
    duration_ms=1000
))

keypress = KeyPressNode("key_1", NodeConfig(
    name="按空格键",
    key="space"
))

root.add_child(ocr_check)
root.add_child(delay)
root.add_child(keypress)

# 执行行为树
engine = BehaviorTreeEngine(root)
engine.start()
```

### 11.2 作为独立应用运行

```bash
python main.py
```

---

## 12. 实现计划

### 12.1 阶段一：核心框架

1. 实现节点基类和组合节点
2. 实现执行引擎
3. 实现黑板系统
4. 实现序列化器
5. 实现节点注册中心

### 12.2 阶段二：基础动作节点

1. 实现按键节点
2. 实现鼠标节点
3. 实现延时节点
4. 实现变量节点

### 12.3 阶段三：条件节点

1. 实现OCR检测节点
2. 实现图像检测节点
3. 实现颜色检测节点
4. 实现数字检测节点
5. 实现变量判断节点

### 12.4 阶段四：扩展功能

1. 实现脚本节点
2. 实现代码节点
3. 实现报警节点
4. 实现脚本录制器

### 12.5 阶段五：脚本录制界面

1. 实现按键命令面板
2. 实现延迟命令面板
3. 实现鼠标命令面板
4. 实现组合按键面板
5. 实现脚本编辑区
6. 实现录制功能
7. 实现脚本执行器

### 12.6 阶段六：设置界面

1. 实现设置管理器
2. 实现 Tesseract OCR 设置
3. 实现报警设置
4. 实现快捷键设置
5. 实现行为树设置
6. 实现界面设置

### 12.7 阶段七：行为树编辑器GUI

1. 实现编辑器主组件
2. 实现画布组件
3. 实现节点面板
4. 实现属性面板
5. 实现工具栏
6. 实现撤销/重做系统

---

## 文档版本

- **版本**: 1.1
- **创建日期**: 2026-04-09
- **更新日期**: 2026-04-09
- **更新内容**: 添加脚本录制界面设计、设置界面设计
- **适用项目**: 独立行为树系统
